/** Horizontal padding aligned with main page content. */
export const pageGutterClassName =
  'px-[max(1rem,env(safe-area-inset-left))] pr-[max(1rem,env(safe-area-inset-right))] sm:px-6';

/** Top padding aligned with main page content (below the app header). */
export const pageTopClassName = 'pt-4 sm:pt-8';

/** Centered content width used in the main column. */
export const pageShellClassName = 'mx-auto w-full max-w-6xl';
