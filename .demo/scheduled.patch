diff --git a/components/campaigns/campaigns-view.tsx b/components/campaigns/campaigns-view.tsx
index 5cb2381..1698585 100644
--- a/components/campaigns/campaigns-view.tsx
+++ b/components/campaigns/campaigns-view.tsx
@@ -1,6 +1,7 @@
 'use client';
 
-import { useState } from 'react';
+import { useState, useEffect } from 'react';
+import { useFlags } from 'launchdarkly-react-client-sdk';
 import { LayoutGridIcon, TableIcon } from 'lucide-react';
 import { Button } from '@/components/ui/button';
 import { FirstFlagDemo } from '@/components/campaigns/first-flag-demo';
@@ -26,8 +27,19 @@ function CampaignGrid({ campaigns }: { campaigns: Campaign[] }) {
 export function CampaignsView({ campaigns }: { campaigns: Campaign[] }) {
   const [status, setStatus] = useState('all');
   const [viewMode, setViewMode] = useState<ViewMode>('grid');
-  const filtered = status === 'all' ? campaigns : campaigns.filter((c) => c.status === status);
-  const statusLabel = STATUS_FILTER_OPTIONS.find((o) => o.value === status)?.label ?? status;
+  const { scheduledStatus } = useFlags();
+  // scheduled-status flag OFF → no Scheduled filter entry, and scheduled campaigns
+  // keep filtering as the draft they present as (StatusBadge applies the same gate).
+  const statusOptions = scheduledStatus
+    ? STATUS_FILTER_OPTIONS
+    : STATUS_FILTER_OPTIONS.filter((o) => o.value !== 'scheduled');
+  const shownStatus = (c: Campaign) =>
+    c.status === 'scheduled' && !scheduledStatus ? 'draft' : c.status;
+  const filtered = status === 'all' ? campaigns : campaigns.filter((c) => shownStatus(c) === status);
+  const statusLabel = statusOptions.find((o) => o.value === status)?.label ?? status;
+  useEffect(() => {
+    if (!scheduledStatus && status === 'scheduled') setStatus('all');
+  }, [scheduledStatus, status]);
 
   return (
     <div className="flex flex-col gap-5 sm:gap-6">
@@ -74,7 +86,7 @@ export function CampaignsView({ campaigns }: { campaigns: Campaign[] }) {
                 <LayoutGridIcon aria-hidden />
               </Button>
             </div>
-            <StatusFilter value={status} onChange={setStatus} />
+            <StatusFilter value={status} onChange={setStatus} options={statusOptions} />
           </div>
           <Button size="lg" className="h-9 w-full shrink-0 whitespace-nowrap sm:w-auto">
             Create campaign
diff --git a/components/campaigns/status-filter.tsx b/components/campaigns/status-filter.tsx
index 45b9066..be2496a 100644
--- a/components/campaigns/status-filter.tsx
+++ b/components/campaigns/status-filter.tsx
@@ -7,12 +7,14 @@ import { STATUS_FILTER_OPTIONS } from '@/components/ui/status-tokens';
 export function StatusFilter({
   value,
   onChange,
+  options = STATUS_FILTER_OPTIONS,
 }: {
   value: string;
   onChange: (value: string) => void;
+  options?: typeof STATUS_FILTER_OPTIONS;
 }) {
   return (
-    <Select.Root items={STATUS_FILTER_OPTIONS} value={value} onValueChange={(v) => onChange(v as string)}>
+    <Select.Root items={options} value={value} onValueChange={(v) => onChange(v as string)}>
       <Select.Trigger
         aria-label="Filter by status"
         className={cn(
@@ -25,7 +27,7 @@ export function StatusFilter({
       <Select.Portal>
         <Select.Positioner sideOffset={6} className="z-50 outline-none">
           <Select.Popup className="min-w-44 rounded-lg border bg-popover p-1 text-popover-foreground shadow-md outline-none">
-            {STATUS_FILTER_OPTIONS.map((item) => (
+            {options.map((item) => (
               <Select.Item
                 key={item.value}
                 value={item.value}
diff --git a/components/ui/status-badge.tsx b/components/ui/status-badge.tsx
index 129ba88..b44f558 100644
--- a/components/ui/status-badge.tsx
+++ b/components/ui/status-badge.tsx
@@ -1,6 +1,7 @@
 'use client';
 
 import { StatusLight } from '@adobe/react-spectrum';
+import { useFlags } from 'launchdarkly-react-client-sdk';
 import { useTheme } from '@/components/theme-provider';
 import { useSpectrumDesignSystem } from '@/components/spectrum-provider';
 import {
@@ -14,18 +15,22 @@ export { STATUS_TOKENS, SPECTRUM_STATUS, type CampaignStatus };
 export function StatusBadge({ status }: { status: CampaignStatus }) {
   const { theme } = useTheme();
   const spectrum = useSpectrumDesignSystem();
+  const { scheduledStatus } = useFlags();
+  // scheduled-status flag OFF → scheduled campaigns present as the draft they were
+  // before the flag, on every surface that renders a StatusBadge.
+  const shown: CampaignStatus = status === 'scheduled' && !scheduledStatus ? 'draft' : status;
 
   if (spectrum) {
-    return <StatusLight variant={SPECTRUM_STATUS[status]}>{STATUS_TOKENS[status].label}</StatusLight>;
+    return <StatusLight variant={SPECTRUM_STATUS[shown]}>{STATUS_TOKENS[shown].label}</StatusLight>;
   }
 
-  const token = STATUS_TOKENS[status][theme];
+  const token = STATUS_TOKENS[shown][theme];
   return (
     <span
       style={{ backgroundColor: token.bg, color: token.fg }}
       className="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold"
     >
-      {STATUS_TOKENS[status].label}
+      {STATUS_TOKENS[shown].label}
     </span>
   );
 }
diff --git a/components/ui/status-tokens.ts b/components/ui/status-tokens.ts
index c77bcf3..b8ba642 100644
--- a/components/ui/status-tokens.ts
+++ b/components/ui/status-tokens.ts
@@ -2,7 +2,7 @@
 // imports) so the a11y gate (status-badge.test.ts) can import it under Node
 // without pulling in Adobe React Spectrum's bundled stylesheets.
 
-export type CampaignStatus = 'draft' | 'live' | 'review' | 'archived';
+export type CampaignStatus = 'draft' | 'live' | 'review' | 'archived' | 'scheduled';
 
 // Source of truth for the legacy StatusBadge path — now the pre-hydration SSR
 // fallback, since Spectrum is the default. Each pair must clear WCAG AA in BOTH
@@ -17,16 +17,18 @@ export const STATUS_TOKENS: Record<
   live: { label: 'Live', light: { bg: '#DCF5E4', fg: '#0F6B33' }, dark: { bg: '#14331F', fg: '#57D98A' } },
   review: { label: 'In review', light: { bg: '#FFF1D6', fg: '#8A4B00' }, dark: { bg: '#3A2A12', fg: '#E0A24E' } },
   archived: { label: 'Archived', light: { bg: '#E7E9EC', fg: '#3A4250' }, dark: { bg: '#2B313B', fg: '#A9B2C0' } },
+  scheduled: { label: 'Scheduled', light: { bg: '#E0EEF9', fg: '#0C447C' }, dark: { bg: '#152C42', fg: '#7EC8F2' } },
 };
 
 // Spectrum path (the rendered default): semantic StatusLight variants,
 // pre-validated AA in light + dark by Spectrum — there is no hand-authored hex
 // left to get wrong. status-badge.spectrum.test.ts asserts these stay semantic.
-export const SPECTRUM_STATUS: Record<CampaignStatus, 'neutral' | 'positive' | 'notice'> = {
+export const SPECTRUM_STATUS: Record<CampaignStatus, 'neutral' | 'positive' | 'notice' | 'info'> = {
   draft: 'neutral',
   live: 'positive',
   review: 'notice',
   archived: 'neutral',
+  scheduled: 'info',
 };
 
 // Filter options + human labels are DERIVED from STATUS_TOKENS — the one
diff --git a/lib/campaigns-seed.ts b/lib/campaigns-seed.ts
index 6631cb8..ce1198c 100644
--- a/lib/campaigns-seed.ts
+++ b/lib/campaigns-seed.ts
@@ -11,7 +11,7 @@ type SeedCampaign = {
   campaignType: string;
 };
 
-/** Canonical campaign seed — keep supabase/migrations/0002 backfill in sync. */
+/** Canonical campaign seed — keep supabase/migrations/0002 backfill + 0007 APJ status in sync. */
 export const CAMPAIGN_SEED: SeedCampaign[] = [
   {
     id: 'c1',
@@ -37,7 +37,7 @@ export const CAMPAIGN_SEED: SeedCampaign[] = [
     id: 'c3',
     name: 'APJ Expansion',
     owner: 'Field',
-    status: 'draft',
+    status: 'scheduled',
     updatedAt: '2026-06-22',
     campaignType: 'Lead generation',
     summary: 'Enterprise trial go-to-market for APJ field teams.',
